ctrl + alt + t - открыть терминал 

ls - просмотр содержимого текщей директорий 
ls - путь до директории - просмотр содержимого указаний директории  

ls -a - просмотр скрытых файлов в директории 

mkdir название директории 
"." ссылка на текущую директорию  

".." ссылка на директорию выше/родительскую 

"-" ссылка на домашнюю диреторию 

pwd - вывод абсолютного пути в консоль

абсолютный путь - путь начинающийся с домашней директорий (home/user/)
оносительный путь - путь относительно других директории (./some_dir)

tree

rmdir удаление пустой директории 

rmdir -rf принудительное удаление директории с содержимым

touch создание файлов с текущей директории 

rm имя файла -  удаление файлов 
tm -rf имя файла - принудительное удаление файла 



cat имя файла - вывод содержимого файла в терминал
cat имя файла >> имя другого файла = дозапись содержимого одного файла в другой 

echo "какой то  текст" - вывод указанного теста в терминал 

echo "какой то текст">> название файла - дозапись указанного текста в файл 

mv название_файла новое_название- переименование 

mv название_файла путь_до_директории - перенос файла в указанную директорию 

nano путь до файла - открытие файла с помощью текстового редактора nano.Если указанного файла не существует то создает его. 

первая_команда && последующая команда - объединение комманд